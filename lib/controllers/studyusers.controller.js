import angular from 'angular';
import studyUserService from './../services/api.service';

export default class StudyUsersController {
  constructor($timeout, api, notifications) {  
  }
  

}

StudyUsersController.$inject = ['$timeout','api','notifications'];